inline void register_log_definitionsTx(
    statespec::backend::ITransaction& tx,
    statespec::backend::ILogSink& sink
)
{
    for (const auto& definition : log_definitions())
    {
        sink.register_definitionTx(tx, definition);
    }
}
