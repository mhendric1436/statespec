inline void register_metric_definitionsTx(
    statespec::backend::ITransaction& tx,
    statespec::backend::IMetricSink& sink
)
{
    for (const auto& definition : metric_definitions())
    {
        sink.register_definitionTx(tx, definition);
    }
}
