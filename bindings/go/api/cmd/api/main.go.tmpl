package main

import (
	api "statespec-generated/api/backend"
	"statespec-generated/common/backend/memory"
)

func main() {
	for _, descriptor := range api.APITierServerDescriptors() {
		_, _ = api.NewDefaultAPITierApplication(memory.NewBackend(), descriptor.Name)
	}
}
