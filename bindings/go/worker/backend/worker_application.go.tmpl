package backend

import (
	"context"

	common "statespec-generated/common/backend"
	descriptortypes "statespec-generated/common/backend/descriptortypes"
)

type WorkerTierApplication struct {
	Context descriptortypes.WorkerContext
	Runtime *WorkerTierRuntime
	Invokers WorkflowStepInvokerMap
}

func (app WorkerTierApplication) RunOnce(ctx context.Context, workflowExecutionID string) (*common.WorkflowExecutionRecord, error) {
	return app.Runtime.RunOnce(ctx, app.Context, app.Invokers, workflowExecutionID)
}
