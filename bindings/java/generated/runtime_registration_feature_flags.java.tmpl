public
static void registerFeatureFlagDefinitionsTx(
    Backend.Transaction tx,
    FeatureFlag store
) throws Backend.BackendException
{
    for (var definition : featureFlagDefinitions())
    {
        store.registerDefinitionTx(tx, definition);
    }
}
