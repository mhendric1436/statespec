{{feature_flag_helpers}}{{lease_helpers}}    public static void ensureSystemCollections(Backend backend)
        throws Backend.BackendException {
{{ensure_system_collections_body}}
    }

{{domain_registration_helpers}}    public static void registerRuntimeCatalogTx(
        Backend.Transaction tx{{tx_parameters}}
    ) throws Backend.BackendException {
{{tx_calls}}    }

    public static void registerRuntimeCatalog(
        Backend backend{{runtime_parameters}}
    ) throws Backend.BackendException {
        var tx = backend.begin();
        try {
            registerRuntimeCatalogTx(tx{{runtime_arguments}});
            backend.commit(tx);
        } catch (Backend.BackendException error) {
            tx.abort();
            throw error;
        }
    }

    public static void bootstrapRuntimeCatalog(
        Backend backend{{runtime_parameters}}
    ) throws Backend.BackendException {
        ensureSystemCollections(backend);
        registerRuntimeCatalog(backend{{runtime_arguments}});
    }
