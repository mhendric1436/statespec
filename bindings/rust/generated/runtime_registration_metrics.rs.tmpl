pub fn register_metric_definitions_tx<B: Backend, S: MetricSink<B>>(
    tx: &mut B::Tx,
    sink: &S,
) -> BackendResult<()> {
    for definition in metric_definitions() {
        sink.register_definition_tx(tx, &definition)?;
    }
    Ok(())
}
