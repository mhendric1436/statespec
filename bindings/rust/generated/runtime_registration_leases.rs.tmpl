pub fn register_lease_definitions_tx<B: Backend, S: LeaseStore<B>>(
    tx: &mut B::Tx,
    store: &S,
) -> BackendResult<()> {
    for definition in lease_definitions() {
        store.register_definition_tx(tx, &definition)?;
    }
    Ok(())
}
