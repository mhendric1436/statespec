use std::sync::Arc;

use crate::backend::BackendResult;
use crate::json::Json;
{{workflow_step_handler_imports}}

#[derive(Clone, Debug, Eq, Hash, PartialEq)]
pub struct WorkflowStepKey {
    pub workflow_name: String,
    pub workflow_version: i64,
    pub step_name: String,
}

impl WorkflowStepKey {
    pub fn encode(&self) -> String {
        workflow_step_key(&self.workflow_name, self.workflow_version, &self.step_name)
    }
}

pub fn workflow_step_key(workflow_name: &str, workflow_version: i64, step_name: &str) -> String {
    format!("{}:{}:{}", workflow_name, workflow_version, step_name)
}

#[derive(Debug, Clone)]
pub struct WorkflowStepHandlerContext {
    pub workflow_name: String,
    pub workflow_version: i64,
    pub step_name: String,
    pub execution_id: Option<String>,
    pub input: Json,
}

pub type WorkflowStepInvoker<B> =
    fn(&dyn WorkflowStepHandlerBundle, &B, &WorkflowStepHandlerContext) -> BackendResult<()>;

pub trait WorkflowStepHandlerBundle {
{{workflow_step_handler_bundle_methods}}
}

#[derive(Clone)]
pub struct DefaultWorkflowStepHandlerBundle {
{{default_workflow_step_handler_fields}}}

impl Default for DefaultWorkflowStepHandlerBundle {
    fn default() -> Self {
        Self {
{{default_workflow_step_handler_initializers}}        }
    }
}

impl DefaultWorkflowStepHandlerBundle {
{{default_workflow_step_handler_setters}}}

impl WorkflowStepHandlerBundle for DefaultWorkflowStepHandlerBundle {
{{default_workflow_step_handler_bundle_impl}}}
