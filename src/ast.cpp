#include "statespec/ast.hpp"

namespace statespec
{

// AST nodes are currently simple value types. This translation unit exists so
// build systems can link a stable statespec library as the AST grows.

} // namespace statespec
